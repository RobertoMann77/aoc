A = np.flip(A,axis = 1)	
			
B = np.split(A, [ 3 ], axis = 1)
		

#numpy array
import numpy as np
size = max(max(x) for x in data) + 1
diagram = np.zeros((size, size), dtype=int) #2-dim array mit 0en
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++